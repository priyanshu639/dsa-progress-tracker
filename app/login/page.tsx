"use client";
import { useState } from "react";import { supabase } from "@/lib/supabase";import { useRouter } from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [signup,setSignup]=useState(false);const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const r=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});setBusy(false);if(r.error)return setMsg(r.error.message);if(signup){setMsg("Account created. If email confirmation is enabled, confirm your email first.");}else router.push("/dashboard")}return <div className="container" style={{maxWidth:520}}><div className="card" style={{marginTop:60}}><h1 className="title">{signup?'Create account':'Welcome back'}</h1><p className="subtitle">Your DSA progress stays tied to your account.</p><form onSubmit={submit} className="grid"><input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Password (min 6 chars)" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/><button className="btn primary" disabled={busy}>{busy?'Please wait…':signup?'Sign up':'Login'}</button></form>{msg&&<p className="success" style={{marginTop:12}}>{msg}</p>}<button className="btn" style={{marginTop:12,width:'100%'}} onClick={()=>setSignup(!signup)}>{signup?'Already have an account? Login':'New here? Create an account'}</button></div></div>}
